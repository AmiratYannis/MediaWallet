import React from "react";

const Video = ({video}) =>{
    console.log(video);
    return (
        <div className="card cardYoutube">
            <h1>Video</h1>
        </div>
    );
}

export default Video;